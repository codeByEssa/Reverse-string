The logic of this, we apply a simply loop on him we set the i = array.length - 1 the total length of his 9 when need of val one by one so we used indexnumbers.
So, the last value ends on 8 and the condition will be if i>=0 then run i-- decrement every time so i becomes 7 after this processs then we used p to display 
the value onto the browser
